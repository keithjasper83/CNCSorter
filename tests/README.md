# Tests Package

This directory contains test files and reference modules for the CNCSorter project.

## Structure

- `modules/` - Reference implementations and original code modules that can be used for testing and comparison

## Purpose

The modules folder contains the original implementation of the vision system that can be:
- Referenced for testing new implementations
- Used as a baseline for performance comparisons
- Accessed by test scripts to validate functionality
