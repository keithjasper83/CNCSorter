"""Setup configuration for CNCSorter package.

This file exists for backward compatibility and editable installs.
Modern configuration is in pyproject.toml.
"""

from setuptools import setup

# Configuration is in pyproject.toml
# This file exists only for editable installs: pip install -e .
if __name__ == "__main__":
    setup()
